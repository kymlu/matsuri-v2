name: 隊列表更新

on:
  push:
    branches:
      - master
    paths:
      - 'intake/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: 依存関係をインストール
        run: npm ci

      - name: ファイル整理
        run: npm run manage

      - name: サイトをデプロイ
        run: npm run deploy